# Hephaestus Current Execution Protocol

Current execution correction: `docs/PROTOCOL_EXECUTION_PRECEDENCE_V2.md`.

This supersedes any older Hephaestus interpretation that turns a later explicit isolated-branch instruction into a request for the same Git authority again. Preserve unrelated protected-effect ceilings, but do the newly authorized reversible work first, verify it, then report.
